services:
  - type: web
    runtime: python
    name: hello-render
    repo: https://github.com/lmmx/hello-render
    region: frankfurt
    plan: free
    buildCommand: poetry install
    startCommand: "ls && hello"
